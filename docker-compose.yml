services:
  backend:
    build: ./backend
    container_name: price-tracker-backend
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - pricetracker-data:/data
    restart: unless-stopped

volumes:
  pricetracker-data:
